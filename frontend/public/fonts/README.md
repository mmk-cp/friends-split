Local fonts used by the app for offline rendering:

- Vazirmatn-Variable.ttf (from Google Fonts repository)
- MarkaziText-Variable.ttf (from Google Fonts repository)

These files are bundled with the frontend so the UI works without internet access.
